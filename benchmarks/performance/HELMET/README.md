# HELMET Experiment

- First, set up the benchmark environment as follows:
    ```
    pip install -e .
    ```

- Then, run the experiments 
    ```
    bash run_fullattn.sh
    bash run_infllmv2.sh
    bash run_nsa.sh
    bash run_dashattn.sh
    ```